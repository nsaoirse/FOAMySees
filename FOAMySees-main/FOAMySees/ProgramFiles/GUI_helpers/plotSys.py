


    #dispop()
    #Getxvec()
    #sysplotax.clear()
    #resplotax.clear()
    #sysplotax.text(L / 3, qo / 900, 'w = ' + str(qo) + '  lbf/foot')
    #sysplotax.plot(xvec, u, 'r')
    #sysplotax.plot(xvec, undeformed, 'k')

    #sysplotax.plot(xvec, np.ones(len(undeformed)) * qo / 1000, 'r')

    #for x in xvec[0::10]:
    #    sysplotax.annotate(" xy=(x, 0), xytext=(x, qo / 1000), arrowprops=dict(arrowstyle="-> color='r'))

    #sysplotax.axis([-L * 0.1, L * 1.1, -L / 100, 1 + L / 50])

    #sysplotax.set_xlabel('X (ft)')
    #sysplotax.set_ylabel('Y (ft)')
    #sysplotax.add_patch(bc1)
    #sysplotax.add_patch(bc2)

    #F1.draw()

    #show()
